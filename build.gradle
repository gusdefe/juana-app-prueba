buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
       classpath 'com.android.tools.build:gradle:8.13.0'
    }
}

// allprojects vac?o - los repositorios se manejan en settings.gradle
allprojects {
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
